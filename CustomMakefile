CC = gcc
CFLAGS = -std=c89 -O3

all: reversi

reversi: build/reversi

build/reversi: build_dir build/main.o
	$(CC) -o build/reversi build/main.o

build/main.o: build_dir main.c
	$(CC) -c $(CFLAGS) -o build/main.o main.c
	
build_dir:
	mkdir -p build

clean:
	rm -rf build

